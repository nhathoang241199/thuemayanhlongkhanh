# /etc/nginx/sites-available/thuemayanhlongkhanh.com
# 1) Copy + enable (tắt default). 2) nginx -t && reload. 3) certbot --nginx -d thuemayanhlongkhanh.com

server {
    listen 80;
    listen [::]:80;
    server_name thuemayanhlongkhanh.com www.thuemayanhlongkhanh.com;

    client_max_body_size 10M;

    location /api/ {
        proxy_pass http://127.0.0.1:3000/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # SePay webhook auth — nginx có thể không forward mặc định
        proxy_set_header Authorization $http_authorization;
        proxy_set_header X-Api-Key $http_x_api_key;
        proxy_pass_header Authorization;
        proxy_read_timeout 60s;
    }

    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}

import { cardSurfaceProps } from "@/lib/app-theme";

/** Card danh sách mobile — cùng surface admin */
export const adminMobileCardProps = {
  ...cardSurfaceProps,
  bg: "white",
  shadow: "sm" as const,
};
